interface:
  display_name: "智慧议会 / Wisdom Council"
  short_description: "先判题，再从 100 位用户自写的历史智者专属 prompt 里选出 10 位，让他们顺序发言并收束成行动方案"
  default_prompt: "Use $wisdom-council to route the right 10 sages from the 100-persona prompt library, inject my dilemma into their original prompts, let them speak in sequence, and synthesize an action plan."
