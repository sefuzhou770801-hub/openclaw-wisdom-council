interface:
  display_name: "智慧议会 / Wisdom Council"
  short_description: "先判题，再从 100 位用户自写的历史智者专属 prompt 里选出 10 位，分别独立调用后再单独总结"
  default_prompt: "Use $wisdom-council to route the right 10 sages from the 100-persona prompt library, run them as 10 independent calls, and then do a separate synthesis call."
